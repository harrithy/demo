# 可搜索的虚拟列表

使用 Vue 3、TypeScript 与 Vite 实现的前端虚拟列表演示。10,000 条模拟数据全部在浏览器中生成，搜索带有 300ms 防抖，列表只渲染可视区域与少量缓冲节点。

## 运行

```powershell
npm install
npm run dev
```

开发服务器启动后会自动打开默认浏览器。

## 可用命令

```powershell
npm run type-check
npm run build
npm run preview
```
